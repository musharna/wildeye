# Gap-layer builder brief (read fully before touching anything)

Repo: ~/wildeye, branch main-wildeye. Node: `export PATH=~/.local/node24/bin:$PATH`. Python: `~/miniconda3/bin/python3 -B`.
Tests: `npm test` (JS, node:test; count `ℹ pass`), `~/miniconda3/bin/python3 -B -m pytest pipeline/tests -q`.

## Read first (the contracts you are cloning — read every file, not just the first)
- Polygon contract: `pipeline/whispers.py`, `pipeline/tests/test_whispers.py`, `src/data/whispers.js`, `src/data/whispers.test.mjs`, `src/data/hpai.js` (binsAt/LIVE_WEEKS), `pipeline/gfw.py` + `src/data/gfw.js` (country polygons, key-in-query-string lesson).
- Site-series contract: `pipeline/otn.py`, `src/data/otn.js`, `pipeline/neon.py`, `src/data/neon.js`.
- Point/occurrence contract: `pipeline/occurrences.py`, `src/data/occurrences.js` (basisText, GROUP_COLORS).
- Raster drape: `pipeline/raster.py`, `pipeline/rasters.json` (only if your source is a raster).
- Runner template: `pipeline/run_whispers.sh` (pins PY, sources `~/.config/wildeye/env` if a key is needed — see `pipeline/run_gfw.sh`).
- Legal ledger format: `DATA_SOURCES.md` rows for WHISPers and GFW (you do NOT edit this file; you RETURN the row text).
- Layer plugin contract: init/enable/disable/update/destroy/getStats/getAnalystRecords, optional setObservedTime(iso|null)/getRowControls/setRowControlsListener. Observed-time bar has a 30-day domain.
- `pipeline/atomic.py` write_atomic for outputs. Seeds live in `public/data/seed/<id>.geojson` and MUST be < 100 KB (subsample/simplify; note the subsampling in the seed's `source` object).

## Hard rules
1. ONLY create/modify: `pipeline/<id>.py`, `pipeline/tests/test_<id>.py`, `pipeline/run_<id>.sh` (chmod +x), `src/data/<id>.js`, `src/data/<id>.test.mjs`, `public/data/seed/<id>.geojson`. Do NOT edit layerState.js, main.js, dataCredits.js, gevActions.js, seed.sh, .gitignore, DATA_SOURCES.md, README, crontab, package.json. Do NOT git commit. Do NOT run pipeline/deploy_pages.sh.
2. Output path: `public/data/<id>.geojson` (gitignored by the integrator). The client layer fetches `data/<id>.geojson?t=`.
3. Licence: read the source's terms LIVE this session (curl the page or API metadata) and quote the licence line. If the source requires sending an email, a manual form, or a click-through you cannot perform, STOP that source and report why — do not fake it.
4. Secrets: never print a key. Keys live in `~/.config/wildeye/env` (KEY=value lines). If your source needs a key that is absent, write the pipeline so it reads `os.environ["<KEY>"]`, exits with a clear message when unset, and report the exact registration URL + the storing command `printf '<KEY>=%s\n' '<paste>' >> ~/.config/wildeye/env` (user pastes; never ask for the value). Still ship the layer with an EMPTY-but-valid seed and everything else complete. Existing keys: MOVEBANK_USER/PASS, XENO_CANTO_KEY, NEON_TOKEN, GFW_API_KEY, CMEMS_USER/PASS, EARTHDATA_TOKEN.
5. Tests: pytest for the pipeline (parse/normalise/aggregate/window logic with fixtures shaped from a REAL response you fetched this session — paste a real snippet, do not invent field names), node:test for the client (class thresholds, binsAt/observed-time, describe HTML, layer contract incl. MultiPolygon parts, getAnalystRecords). Each test must be seen to fail on a deliberate mutant before you accept it (state the mutant in a test docstring/comment). A negative assertion needs a positive control in the same test.
6. Real execution: run `pipeline/run_<id>.sh` under `env -i PATH=/usr/bin:/bin HOME=$HOME bash pipeline/run_<id>.sh` and confirm the output file is non-empty and well-formed; then drive the client layer under node against the real file (mock fetch to read the file; assert feature count > 0 and getStats has no error). Report counts.
7. Rate limits: ≥2 s between paged requests to unfamiliar hosts; UA header naming the repo (`wildeye (github.com/musharna/wildeye)`).
8. Fail loud: no silent fallbacks; 5xx retry with backoff is fine (see otn.py `_get_json`).
9. Cheap scripting only — no GPU, no jobs > 15 min. Bound one-shot python with `timeout`.
10. Do not publish Artifacts. Do not create GitHub repos.

## Report back (this is all the integrator sees)
- id, token (given), layer `name`, `icon`, contract used, data window, feature count from the live run, seed size.
- The DATA_SOURCES.md row text (same column layout as the WHISPers row).
- A dataCredits.js entry (`key`, `html`) and 3–5 voice aliases for gevActions.js.
- Exact licence line quoted + URL, and any key registration URL + storing command.
- Test counts (pytest, node) and the mutants you verified.
- Anything declined and why.
