# Lessons

One line per miss: the class of miss and the mechanism that now catches it.

- 2026-09-18 (R13-M3): a client method made a second request for data only one caller shows, adding a failure path for callers that never use it; the lookup moved to the caller that shows the name, pinned by a test that maps an EXACT synonym while the lookup fails.
- 2026-09-18 (R13-M4): only one of two entry points advanced the cancellation generation, so a stale async answer overwrote a newer choice; every entry point now starts a new choice, pinned by a race test and a test that fails when the lookup drops its abort signal.
- 2026-09-18 (R13-M5): a key handler on one part of a composite widget (the combobox input) let the same key from its other part (the listbox) reach document listeners; the handler is on both, pinned by a unit test and the qa `escape` Tab-to-suggestion step.
- 2026-09-18 (R13-M8): a container that fills with long content was itself an aria-live region; the card is labelled instead and a separate one-line status region announces, pinned by a markup test.
- 2026-09-19 (R13-M1): a window-height media query decided how a card split its height between two lists, but the split depended on content the query cannot see (failure notes, wrapping); a grid now shares the height by content with floors, pinned by qa `card-foot-rest`'s share assertion at seven sizes and a unit pin that no height query styles the card.
- 2026-09-19 (R13-M2): a scroll container with no padding clipped its children's focus rings; the rows are inset by the ring's reach, pinned by qa `card-foot-rest` focusing the first and last link and measuring the ring against every clip.
- 2026-09-19 (brief B S-1, fold): a weak fade was the only "more below" cue, and variable-height lines above a primary control pushed it out of a small view; the panel's cue mechanism is shared (moreCue.js) and the control comes first, pinned by qa `card-foot-rest` (cue) and qa `panel-fold`.
- 2026-09-19 (brief B pills): shared label text at 0.3 white was never measured because the contrast check parked the host header; qa `collapsed-pills` now measures every pill's label over the light map and ocean, and qa `contrast` gates the header.
- 2026-09-19 (brief B qa): a qa check's forced-failure state assumed an empty session cache, so an earlier check's cached successes hid the failures and the state never settled, which had been put down to the network; the check now forces failures on keys no cache holds.
- 2026-09-19 (A review I-2): guards after each await were written but only one path was tested, so two of them survived removal; a test per guard (late EXACT match, pick during the map switch) now fails when either is removed.
- 2026-09-19 (A review m-1/m-2): replacing a live region with a one-line announcer dropped what the region used to say (which record, which lookups failed) and repeated identical text; the line names the record and the failures and re-sets identical text, pinned by unit tests and qa `card-announce`, `card-announce-repeat` and `card-foot-rest`.
- 2026-09-19 (A review m-3): the handled-key convention (preventDefault) was honoured by some document Escape handlers and not by the trackers, so one Escape did two things; the five tracker handlers skip handled Escapes, pinned by a vessel wire test and escapeHandled.test.mjs.
- 2026-09-19 (A review m-4): a lookup that only feeds a note was made a hard failure of the whole choice; the taxon now maps by key and the note says the lookup failed.
- 2026-09-19 (B review I-1): a qa check waived its own requirement ("≥1 species row") by a quantity measured from the build under test, so a regression could turn the waiver on; the floors are pinned constants in qa `card-foot-rest` and no size waives the species row.
- 2026-09-19 (B critic S1/S2): two layout fixes moved the loss instead of removing it (landscape kept the secondary parts and dropped the species; the fold moved onto the chosen species); the floors now encode priority and the phone accordion gives the SPECIES body its view, pinned by qa `card-foot-rest` at 667x375 and qa `panel-fold` requiring search, status, chosen row and action whole.
- 2026-09-19 (B critic S3): removing a scroll fade left a line sliced at a hard edge; qa `card-foot-rest` now requires every cut line to sit inside a fade.
- 2026-09-19 (B review I-2): a claim scoped to "every request" was checked only in the app's JS, not the pipeline's Python; the pipeline query names the checklist, pinned by a pytest.
- 2026-09-19 (final review m-1): an accordion done in presentation only (hide siblings while SPECIES is open) hid panels that other paths opened (a share link); the phone accordion is enforced in setPanelCollapsed, the path every opener takes, pinned by a unit test of the two-panel share restore and qa `phone-accordion`.
- 2026-09-19 (final review m-2): a qa step switched to a script click to get past one size, and so stopped checking that people can reach the control at every size; it clicks for real and fails with what it hit.
- 2026-09-19 (final review m-3): an announcer outside the element it describes kept speaking when that element was hidden by a page mode; it checks the card is rendered, pinned by a unit test and qa `card-announce-hidden`.
- 2026-09-19 (critic r1 N1): contrast measured by specified colour passed while thin rendered strokes did not; the label colour is set by the rendered-pixel measurement, recorded in the CSS comment and the unit pin.
- 2026-09-19 (critic r2 S1): the qa step before the globe click collapsed SPECIES itself, so it could not see that the armed pick left the panel over the centre; it now clicks as the page leaves it and fails with what covers the centre.
- 2026-09-19 (critic r2 S2): a fix for one size (the landscape card's focused link) was written unscoped and changed every size; it is scoped to the short-window block, and a unit pin forbids the focus share outside it.
- 2026-09-19 (fix round 3): qa screenshots from a second tab were taken with the main page's helper, so the "landscape" images showed the desktop page; the check screenshots its own tab.
- 2026-09-19 (critic r3, fix round 4): three rounds of per-size patches in short landscape each moved an overlap somewhere new, because every fix checked only the size it was written for; the short viewport is now one model (regions for stack, card and pick target, one query in style.css and shortViewport.js), and qa landscape-regions checks every rule of it at three sizes with real clicks.
- 2026-09-19 (critic r4 B1): the short-viewport model was scoped by width as well as height, so the most common phone-landscape sizes (wider than 720 px) fell to the desktop lane, which gave an open panel 38 px (on main too); the model is a height condition at every width, and qa landscape-regions runs at 740x360, 844x390, 932x430, 1024x500 and 1024x580.
- 2026-09-19 (critic r4 S1): an "impossible" exemption was accepted while the card kept secondary parts (dataset rows, a note) over the primary one (the species list); the card folds the secondary parts first, and the qa waiver is gone.
- 2026-09-19 (critic r5 S1): layout positions were constants measured at one window size (a stack top of 70 px), and the header grew with wider windows; the short columns now start below the header boxes measured in the page, and qa landscape-regions checks the header's text against every pill and panel.
- 2026-09-19 (critic r5 S2): a rule written for where two regions meet ("take turns") was applied where they do not, taking away a working side-by-side layout; it now acts only when the boxes would intersect or the panel covers the pick target, and qa checks both outcomes.
- 2026-09-19 (critic r5 N1): information moved into a title attribute is out of reach on touch; the folded note has an info button, tapped for real in qa.
- 2026-09-20 (deploy): the deploy published the site as a fresh orphan commit and force-pushed it, so every run re-uploaded all 108 MB over an uplink that drops pushes near 50 MB; the visible cost was not a slow push but a dataset silently cut to fit it — the birds replay archive shipped 2 of its 31 nights. Publishing on top of the remote tip makes the push a delta (3.4 MB measured on the first run, landing on attempt 1 where the previous four deploys each needed two or three), and the script now refuses to deploy when ls-remote fails rather than reading a network error as a first deploy, and aborts on a parentless tip rather than quietly re-orphaning.
- 2026-09-20 (qa-observed-time, self-inflicted): a new check reported ok:true while asserting nights >= 3 against a 2-night site, because report() spread its detail payload after the verdict and that payload carried its own `ok`; the verdict is now written last, and the control that proved it was a 2-night copy of the real site served locally — a check first seen to fail is the only kind worth the line it is written on.
- 2026-09-20 (observedTime): a subscriber asked the manager's TRANSITIONAL event for SETTLED state — `visibility-transition` only ever carries lifecycleState 'enabling'/'disabling' (manager.js:696 is its one call site) while the settle that writes 'enabled' notifies nobody, so the branch that hands a newly enabled layer the scrubbed time never ran in a browser and a layer switched on while scrubbed showed live data under a past timestamp; settled visibility is `{type:'visibility', enabled}`. The unit test could not catch it because the fixture synthesised the very event the manager never sends — the branch looked covered. Both behaviours are now pinned by tests that drive the REAL DataLayerManager, the only control that can falsify a belief about another module's event contract.
- 2026-09-22 (observedTime domain): the scrubber's span was a 30-day constant applied to `now`, invented in the store and never compared with a layer, while the birds archive is a fixed backfill nothing advances — so the bar's right edge addressed 2026-09-22 05:00 UTC when the newest frame was 2026-09-10 12:00 UTC, and the layer answered by drawing the old frame under the new label with every request returning 200. Two windows specified independently drift apart, so every check that asked "did a frame load, was it 200" passed on the broken build; the assertion that can fail compares the bar's right edge with the manifest's last frame (281 hours on 7fb7dee, 0 after). Each layer now declares getObservedExtent() and the domain is the union, so the span cannot disagree with the data; the same constant had also been hiding occurrences records older than 30 days. The comment in birds.js claiming "the cron appends a frame every hour" was the belief the defect rested on — a stale comment is a fixture for the next reader.
- 2026-09-22 (GIBS review I1): the observed-time bridge pushed to a layer on enable only when the bar was scrubbed, and never to a disabled layer, so a layer switched off in the past and back on under LIVE kept drawing the past under a LIVE label; the bridge now records what it told each layer, and the test drives off→LIVE→on.
- 2026-09-22 (GIBS review I2): an error flag set by one tile provider outlived it — "map tiles failing" stayed after the date changed and after every tile was 200 again, and nothing re-requested failed tiles; the flag now belongs to the provider generation and a refresh rebuilds a failing provider, tested failing → new date → failing again.
- 2026-09-22 (GIBS review I4): a commit said the pipeline "fails loud when a colour map is gone" but only a failed fetch raised; a vanished link wrote a caption-only legend and exited 0, and the QA legend check (rows > 0) passed it. Which layers must have a colour map is now data (LAYERS), tested by deleting the link from the real capabilities fixture.
- 2026-09-23 (GIBS stage 2 compare): the pill passed every unit test and qa-compare's "on screen" box check while the command dock's voice widget covered its centre, so a real click hit the dock; a box that exists is not a box you can click. qa-compare now clicks through puppeteer (hit-tested), and the pill is placed above measured chrome — and the same probe showed the stage-1 time bar's slider centre is under the dock too (unfixed, reported).
- 2026-09-23 (GIBS stage 2 compare): compare.set() checked for a newer set only after both enables, so a superseded call enabled its own stale side and the one-drape rule ended the live compare; async setters that can be superseded check ownership before every side effect, pinned by "a newer set wins: a superseded set enables nothing further".
- 2026-09-23 (GIBS stage 2 review I1): the compare labels re-rendered on restack and manager events only, but a scrub into a data gap reaches a layer through setObservedTime and hides its imagery with neither, so a side kept its old date over an empty half-globe; a view of layer state must subscribe to every path that changes it — pinned by the real-store test "a scrub that puts a side into a gap relabels it".
- 2026-09-23 (time bar under the dock): every qa-observed-time check drove the slider with dispatchEvent, which reaches an element through whatever covers it, so the bar passed 7/7 live while the command dock covered its middle; a UI control is checked by a real pointer click after an elementFromPoint hit-test along its length (slider-clickable). Moving it clear of the dock then put it on the map credits at 390px — placement is one measured stack above all bottom chrome (bottomStack.js), not an offset from one neighbour.
- 2026-09-23 (GIBS stage 3 review I1): a readout printed each bin's midpoint at a precision taken from the bin width, so at 0 dp GEDI's [12,13) showed 13, outside its own bin — and the test asserted that value because I wrote the expectation from the code's arithmetic; value-formatting tests now sweep every bin of the real tables and assert the shown value lies inside it.
- 2026-09-24 (S3 pilot site check): the sampler wrote each point at 5 dp, but a level-8 land-cover pixel centre lies exactly on a level-9 EVI pixel edge, so the live readout read the neighbouring EVI pixel for 32 of 50 points; coordinates that cross between grids are serialised at full precision (repr), pinned by test_written_coordinates_read_back_into_the_same_pixel_at_every_level, and the offline-vs-site agreement check is what caught it.
- 2026-09-24 (S3 season figures): the R script computed `testable`/`passes` as loose vectors and then re-sorted the region table, so the curve figure silently dropped Cairo and drew Manaus, and its subtitle named the wrong region; per-row flags live as columns of the table they describe, and the rendered figure is read against the CSV before commit (that read is what caught it).
- 2026-09-24 (time bar on phones): every qa-observed-time check ran at 1400x900, so a bar of fixed inline minimums that was 616 px wide and started off-screen at 390 px passed 8/8; the bar's layout moved to style.css (an inline width cannot answer a media query) and fits-viewport now hit-tests every control at 390x844, 844x390 and 1400x900. My first fix then halved the bar's room — a fixed box centred with left:50% shrink-fits into half the viewport, so wrapping put the desktop bar on three rows while all checks passed — so the same check asserts one row above the phone breakpoint, and hit-tests wait two animation frames (the stack re-places from a ResizeObserver, before paint).
- 2026-09-24 (paint-queue sort): sortPooledRange's docstring asserted the frame path sorts "a short, nearly sorted prefix", and nothing measured it; the all-live queue was ~350 items at ~n²/3.4 inversions, so an insertion sort spent ~36k comparisons a frame. The call-count ratchet (scripts/callcount-check.mjs) exposed it, and pooledSort.test.mjs now bounds comparisons on disordered input, so a cost model stated in prose is checked by a count.
- 2026-09-24 (command dock on phones): the narrow tray offset was written as arithmetic over the dock's assumed parts (two tabs + voice); the fork dropped the voice control and every tray hung 70 px off-centre with nothing failing, and `position: static` alone did not move the containing block because the tab's `backdrop-filter` makes it one. Place a popover against the box it must centre on, never a sum of its siblings' sizes; `scripts/qa-command-dock.mjs` now opens each tray by a real tap at 390/480/844/1400 and requires the tray, its first control and its pin wholly on screen, measured once the tray has settled (a fixed sleep caught it mid-transition).
- 2026-09-24 (startup-weight gate): the first cut classified vendor code by chunk FILE name (which cannot see a scoped package and collapses same-named chunks) and cut the file set at the ready signal (a race: the HUD's first-tick geoid fetch landed either side of it); the gate now keys by the build manifest's source module and measures until startup settles, and runs two loads per check so a race fails as "disagreed" instead of flaking.
- 2026-09-24 (geoid grid seam): a seam test compared geoidHeight(lat, 180) with geoidHeight(lat, -180), which normalize to the same longitude before the lookup, so it compared a value to itself; review caught it. Edge tests now probe just inside each edge so they read different data, and each one is seen to fail on a mutant of the edge it names.
